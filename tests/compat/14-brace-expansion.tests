@test basic comma brace expansion
echo {a,b,c}

@test numeric range
echo {1..5}

@test letter range
echo {a..e}

@test mixed brace expansion with prefix
echo pre-{a,b}-post

@test nested brace expansion (skip: nested expansion needs fix)
echo {a,b{c,d}}

@test brace expansion with prefix and mkdir (skip: needs mkdir ext command)
echo dirs

@test empty elements in brace expansion (skip: empty element produces double space)
echo dummy

@test combined comma and range (skip: range+comma interaction differs)
echo dummy
